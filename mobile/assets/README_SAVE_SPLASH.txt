Place the splash image provided by the designer here:

Path: ./assets/splash.png

- Save the PNG (or JPG) with the filename `splash.png`.
- Recommended size: 1242x2436 (or large enough for modern phones). Keep aspect ratio.
- The `app.json` is configured to use this file for the Expo splash screen.

If you want me to add the actual binary image file, upload it and I'll place it here as `assets/splash.png`.
